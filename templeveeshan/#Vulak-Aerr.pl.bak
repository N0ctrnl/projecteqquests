sub EVENT_DEATH_COMPLETE {
  quest::signalwith(10,1,0);
#  quest::disable_spawn2(111236);
}

sub EVENT_AGGRO {
  my $guards = $entity_list->GetMobByNpcTypeID(124157);

  if ($guards) {
  my $guards = $guards->CastToNPC();
  $guards->AddToHateList($client, 1);
}
 }